export const configuration = () => {
  const {
    NODE_ENV,
    APP_PORT,
    JWT_SECRET,
    JWT_EXP,
    JWT_REFRESH_SECRET,
    JWT_REFRESH_EXP,
    GOOGLE_CLIENT_ID,
    GOOGLE_CLIENT_SECRET,
    GOOGLE_REFRESH_TOKEN,
    EMAIL,
    EMAIL_CONFIRMATION_URL,
    BASE_IMAGES_URL,
    DB_HOST,
    DB_PASSWORD,
    DB_PORT,
    DB_NAME,
    DB_USERNAME,
  } = process.env || {};

  return {
    NODE_ENV,
    APP_PORT,
    JWT_SECRET,
    JWT_EXP,
    JWT_REFRESH_SECRET,
    JWT_REFRESH_EXP,
    GOOGLE_CLIENT_ID,
    GOOGLE_CLIENT_SECRET,
    GOOGLE_REFRESH_TOKEN,
    EMAIL,
    EMAIL_CONFIRMATION_URL,
    BASE_IMAGES_URL,
    DB_HOST,
    DB_NAME,
    DB_PASSWORD,
    DB_PORT,
    DB_USERNAME,
  };
};
